source "https://rubygems.org"

# GitHub Pages gem — pins Jekyll + plugins to the versions GitHub Pages uses,
# and bundles jekyll-remote-theme (for remote_theme: vsoch/mkdocs-jekyll).
gem "github-pages", group: :jekyll_plugins

# Needed to `jekyll serve` on Ruby 3.x locally.
gem "webrick"
